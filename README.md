# 🍎 Calorie Tracker

**Calorie Tracker** — это мобильное приложение на Flutter, которое помогает пользователям следить за своим питанием. Приложение позволяет записывать приемы пищи, отслеживать количество потребленных калорий за день, просматривать историю и настраивать интерфейс под себя.

## ✨ Основные возможности

* **📊 Дневной обзор**: Отображение списка съеденного за сегодня и общей суммы калорий.
* **➕ Добавление и Редактирование**: Удобная форма для добавления новых блюд (название, калорийность) и возможность редактировать уже созданные записи.
* **📅 История**: Просмотр статистики питания за прошедшие дни.
* **⚙️ Настройки**:
    * Переключение между **Темной** и **Светлой** темами.
    * Возможность очистки всех данных.
* **💾 Локальное хранение**: Все данные о приемах пищи сохраняются в JSON-файл на устройстве, что обеспечивает работу без интернета и конфиденциальность.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Хранение данных**:
    * `path_provider` — для доступа к файловой системе устройства.
    * **JSON** — формат сериализации данных для хранения истории питания.
* **UI/UX**:
    * Поддержка Material Design 3.
    * Кастомная реализация тем (`lib/themes`).
    * Навигация через `BottomNavigationBar` (Главная / История / Настройки).

## 📂 Структура проекта

Код организован по функциональным модулям:

```text
lib/
├── models/           # Модели данных
│   └── meal_model.dart      # Модель блюда (название, калории, дата)
├── screens/          # Экраны приложения
│   ├── calories_screen.dart # Главный экран (список за сегодня)
│   ├── edit_meal_screen.dart# Экран добавления/изменения записи
│   ├── history_screen.dart  # Экран истории по дням
│   ├── settings_screen.dart # Экран настроек и тем
│   └── home_screen.dart     # Корневой экран с навигацией
├── themes/           # Темизация
│   ├── dark_theme.dart      # Настройки темной темы
│   └── light_theme.dart     # Настройки светлой темы
├── utils/            # Утилиты
│   └── calorie_tracker_storage.dart # Логика чтения/записи JSON файла
└── main.dart         # Точка входа
